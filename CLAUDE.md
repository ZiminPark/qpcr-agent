# qPCR Agent — 발표 데모

가상 실험실 데모. 챗봇에 도구를 단계별로 붙여 qPCR Agent로 키우는 라이브 시연이다.

## 실행

- 서버: 단일 프로세스, 포트 8000 고정 (MCP `/mcp/<stage이름>` + 대시보드). uv 사용
- 발표 중 서버는 재시작하지 않는다. 상태 초기화는 `/admin/reset`
- 서버 코드에 난수 금지 (결정론 — 데모는 항상 같은 그림이 나와야 한다)
- 화면·대사에 어려운 실험 용어를 쓰지 않는다. 꼭 필요한 것(Cq, GAPDH, well 정도)만 남기고 일상어로
- `stages/` 간 파일 중복은 의도적(단계 증분을 diff로 보여주기 위함). 공통화·심볼릭 링크로 "정리"하지 말 것
