# 발표 데모 구현 스펙 결정 로그

그릴링 세션(2026-08-31)에서 정한 순서대로. 근거는 발표 기획 문서와 대화 기록 참조.

1. **repo 범위**: 이 repo 하나에 발표 자산 전부(서버, 대시보드, stage 디렉터리, 시나리오, MHS, 시작 킷)를 담는다. 이 repo가 청중 공유용 시작 킷을 겸하므로 공개 곤란한 내용물은 만들지 않는다.
2. **서버 구성**: Python 단일 프로세스. MCP는 streamable HTTP, 같은 프로세스에서 대시보드도 서빙. 기기 상태는 인메모리 상태 딕셔너리 하나. 서버는 발표 내내 상시 실행, stage 전환은 새 Claude Code 세션.
3. **stage 이름**: `stageN_이름` 형식 8개 (chatbot/read/write/memory/loop/guardrail/multiagent/mhs). 최종 무인 완주는 stage6에서 실행.
4. **stage → 도구 세트**: 서버가 stage 이름 전체를 받고 내부 매핑 테이블로 도구 세트를 결정 (A안). `stage0_chatbot`은 MCP 접속 자체를 안 한다. safety limit은 상시 켜져 있다 — stage5는 서버가 바뀌는 단계가 아니라 위반을 처음 시도하는 단계.
5. **MCP 표면**: tools만 쓴다. resources/prompts는 쓰지 않는다. 발표자 대본 프롬프트는 각 stage 디렉터리의 `.claude/commands/` 슬래시 커맨드로 해결.
6. **디렉터리 구조**: `server/`(devices/, dashboard/, scenarios/) + `stages/stageN_이름/` + README(시작 킷 가이드 겸용).
7. **웹 프레임워크**: FastAPI + 공식 MCP SDK(FastMCP). 도구 세트별 FastMCP 인스턴스를 `/mcp/<stage이름>` 경로에 mount (같은 세트는 공유 mount). session manager lifespan을 FastAPI lifespan에 연결할 것.
8. **대시보드**: 빌드 스텝 없는 vanilla JS 한 장 + SSE push + CDN 차트 라이브러리. 가독성 우선(큰 글씨), 장식 없음.
9. **도메인 모델**: 명사 6개(샘플/합격 기준/작업 목록/플레이트/well/시약 잔량)만. 서버는 숫자만 만들고 해석은 Agent 몫. 실험은 one-step RT-qPCR과 IL6/GAPDH 2채널 multiplex를 전제로 하되, 데모에는 IL6 곡선과 GAPDH Cq만 노출한다. 상세는 CONTEXT.local.md 용어집.
10. **도구 인벤토리**: read(read_nanodrop, check_devices) ⊂ write(+run_liquid_handler, reserve_quantstudio) ⊂ qpcr(+start_qpcr, get_qpcr_curves) ⊂ mhs(+discover_devices, read_device, run_device). 도구 세트는 발표 진도에 맞춰 Agent 능력을 잠그는 장치이기도 하다. 보고서 도구는 없음(Agent가 파일로 직접 씀). stage1에 check_devices를 추가해 0단계 불편 두 개를 모두 회수.
11. **qPCR 런 시간**: 실제 런 시간을 발표용으로 압축해 40사이클 × 2초 = 80초로 표현한다. 사이클 속도는 서버 설정값. 이상 well은 ~25사이클부터 잠정 감지하되, 재검 판단은 런 완료 후 한다.
12. **결정론**: 난수·시드 없이 모든 숫자를 시나리오 파일에 박음 (`week1.json`, `week2.json` — 파일명·배치 구조는 결정 17에서 확정, 초안의 week_normal/week_x3는 폐기). 배치마다 샘플 12개 중 2개 QC 탈락. 이상 well은 ⓐ(증폭 실패)가 week1에, ⓑ(3반복 중 1개 튐)가 week2에 각 1개 (결정 17의 배치 분할). 리셋은 `/admin/reset` + 대시보드 버튼, MCP로는 미노출. stage 간 상태는 이어간다 (발표 전체가 연속된 한 실험).
13. **trace 파이프라인**: 이벤트 3종만 — Tool 호출(서버가 직접 기록 + 한글 설명 매핑), 사용자 프롬프트(`UserPromptSubmit` hook → `POST /trace`), 사람 승인(`PreToolUse` hook "승인 대기" → `PostToolUse` hook 도착 시 "승인됨"). 각 이벤트에 발생 시각 필드 포함(서버 실제 시각 기록). 🤖 Agent 판단 카드는 뺀다 — 좌(터미널)=Agent의 뇌, 우(대시보드)=실험실이라는 분업 유지. **구현 메모**: stage hook은 Claude Code의 원본 hook JSON(`hook_event_name`/`prompt`/`tool_name`/`tool_input`)을 변형 없이 `curl -d @-`로 넘긴다(jq 등 추가 의존성 없이) — `POST /trace`가 `hook_event_name`을 보고 직접 해석한다. 승인 id는 서버가 `session_id+tool_name+tool_input` 해시로 결정론적으로 만들어, PreToolUse가 만든 카드를 같은 호출의 PostToolUse가 찾아 갱신한다. 계약 상세는 `server/dashboard/API.md`.
14. **stage 디렉터리 규격**: `.mcp.json`(1~7) / `.claude/settings.json`(hooks + 읽기 allow·쓰기 확인 + `enabledMcpjsonServers: ["qpcr_lab"]` — 디렉터리마다 뜨는 .mcp.json 승인 프롬프트 생략 + Read deny 3종 `//**/qpcr_agent/{docs,server}/**`·`CONTEXT.local.md` — stage 세션이 상위 디렉터리의 스포일러(시나리오 숫자·결정 로그)를 읽고 "아는 척"하는 것을 차단. Claude Code의 프로젝트 경계가 git 루트라 repo 전체가 읽기 허용되므로 deny로 막아야 하며, 상대 패턴·`..`는 안 먹혀 와일드카드 절대 패턴을 쓴다(폴더명 qpcr_agent 기준, 2026-08-31 추가) / `.claude/commands/`(대본) / stage0에 `results_week1.csv`(구 week0, 2026-08-31 개명) / stage3+에 `CLAUDE.md`(@lab_notebook.md import 한 줄) + `lab_notebook.md` / stage6+에 `.claude/agents/qc-reviewer.md`. stage 간 파일 중복은 의도적(스냅샷·diff 가능성). 정합성은 `scripts/check_stages.py`로 검증.
15. **대시보드 레이아웃** (mock: `server/dashboard/mock.html`): 세로 3단 — 기기 카드 줄(= 연결된 기기 목록, MHS에서 카드가 늘어남) / 메인 뷰(플레이트↔곡선 자동 전환, 발표 중 무조작) / trace 타임라인. 섹션 헤더는 박스 밖으로 분리(20px, 위 마진 28px); 메인 뷰 헤더는 제목만("증폭 곡선"/"플레이트"), 상세 정보(사이클 진행 등)·전환 컨트롤은 박스 안 서브 행 — 모드 전환 시 제목과 상세를 각각 갱신. 곡선은 정상 회색·이상만 빨강, 이상 표시는 Agent 판정 이후에만(스포일러 방지 — 구현: Agent가 `get_qpcr_curves`로 사이클 25 이상 시점의 곡선을 실제로 읽은 뒤에만 `display_hint`가 켜진다, 단순 경과 시간 아님). 곡선·결과표에 보이는 형광/Cq 숫자 자체는 시나리오 파일 값 그대로 대시보드에 내려준다(결정 12) — 감추는 건 "이상이다"라는 해석(display_hint)뿐이다. trace는 카드형·크기 균일, 타이틀 줄(이벤트 종류 한글 + 오른쪽에 시:분:초 타임스탬프, tabular-nums), **역시간순(최신 위)** — 새 카드가 헤더 바로 밑 고정 위치에 나타나 스크롤 없이 최신 이벤트가 보이도록. 타임스탬프는 위→아래 단조 감소해야 함(순서 검증 겸용). 최신 항목 크게(23px/17px/14px) 강조는 폐기.
16. **stage5 guardrail**: safety limit은 시약 잔량 하나만. 거부는 실행 전·부분 실행 없음. 에러 응답에 이유+숫자(requested/available)를 담아 Agent가 계획을 수정할 수 있게 함. 거부도 trace 카드로 표시. buffer 초기값은 정상 소모량의 ~1.5배 (정상 통과, 5배 지시는 확실히 거부).
17. **stage6 multi-agent**: 병렬 worker 증설이 아니라 "종류가 다른 Agent의 등장"에 집중. 역할 3종/플레이트 1장 — orchestrator(메인 세션, 위임·보고서), worker(범용 Task subagent, 읽기+쓰기), qc-reviewer(정의 파일 1개, 읽기 전용·신선한 컨텍스트). QuantStudio 1대 유지, week_x3 폐기. 시나리오에 배치 2개(week1: stage0~5, week2: stage6 무인 완주·이상 well ⓑ 포함) + "다음 배치 투입" admin 조작. 병렬성은 슬라이드 한 줄 언급만. 기획서 6단계 서사 수정 반영 완료(2026-08-31).
18. **결과·보고서 형식**: 결과 CSV는 샘플당 1행, 3반복을 열로 펼침(IL6_cq_1..3, GAPDH_cq_1..3) — ⓑ 이상이 표에서 바로 보임. 0단계 소품과 qPCR 결과가 같은 형식. 보고서는 Agent가 마크다운 직접 작성: 결론/샘플별 결과(S01 대비 fold change)/합격·탈락/특이사항 4절. 완료 기준 체크리스트는 lab_notebook.md의 랩 규칙으로. **구현 메모**: 검증된 one-step RT-qPCR 시약과 IL6/GAPDH 2채널 multiplex assay를 사용한다. 플레이트는 well 3칸/샘플 규칙(CONTEXT.local.md)을 유지 — GAPDH용 별도 well을 만들지 않는다. 데모 단순화를 위해 시나리오 파일이 well마다 2채널 중 GAPDH의 장비 계산값 `gapdh_cq`를 함께 갖고 있고, `get_qpcr_curves` 응답에 그대로 실린다. IL6(목표 유전자) Cq는 그 well의 IL6 형광 곡선에서 Agent가 임계값 교차로 직접 계산한다(결정 9).
19. **희석 스펙**: 랩 규칙 "투입 농도 50 ng/µL, 샘플당 최종 50 µL" (lab_notebook.md 소유). 계산은 C1V1=C2V2 하나. worklist = 샘플당 2건(원액+buffer) × QC 통과 10샘플 = 20건. 샘플 농도는 60~200 ng/µL(희석만, 농축 없음). 부피 단위는 µL로 통일 — buffer 초기 잔량 2000 µL(리허설에서 준비를 여러 번 다시 돌릴 여유, 2026-08-31에 525에서 상향), 5배 지시는 거부 — 실제 숫자는 요청 ~1,693 µL > 시점 잔량 ~1,661 µL(2단계 소모 ~339 µL 후, 여유 ~32 µL). **주의**: 준비를 한 번도 안 돌린 신선한 상태(잔량 2000)에서는 5배 요청이 통과해버리므로, stage5 시연은 반드시 2단계 준비 이후에 할 것.
20. **대시보드 결과 표시**: 메인 뷰 3모드 자동 전환 — 플레이트 → (런 중) 곡선 → (런 완료) 결과표. 보고서 패널은 v1 제외 (필요해지면 PostToolUse hook으로 추가).
21. **lab_notebook.md**: "김 연구원이 자기 보려고 쓴 메모" 톤 — IT 청중이 바로 이해하도록 전문 용어는 괄호에 한 번만 두고 일상어로 풀어 쓴다. 3칸 반복, RNA 변환부터 측정까지 한 번에 하는 시약, 한 칸에서 IL6/GAPDH 동시 측정, GAPDH의 보정 역할, S01 비교 기준, 농도 50·총량 50µL, 순도 1.8 불합격과 측정 완료 후 재검 판단을 적는다. 보고서 쓰는 법(결론→표→불합격 목록, 이유 필수), 지난주 메모(lot #2408, S07·S11 불합격)도 포함한다. v1에서 읽기 전용. 청중이 3초에 훑는 소품.
22. **콘텐츠 난이도 원칙**: 청중·화면에 노출되는 모든 텍스트는 IT 담당자 기준. 실험 전문용어는 데모 대사에 필요한 것(Cq, GAPDH, well 등 소수)만 남기고 일상어로.
23. **Python 툴체인**: uv + 의존성 3개(fastapi, uvicorn, mcp). 포트 8000 상수 고정 (.mcp.json 8개가 참조하므로 설정화하지 않음).
24. **worklist 표기**: 화면·대사·에러 메시지 등 노출 텍스트에는 "작업 목록"만 쓴다(괄호 병기도 안 함). `worklist`는 `run_liquid_handler` 파라미터명·state 키 등 코드 식별자로만 유지. (2026-08-31)
25. **루트 CLAUDE.md는 얇게**: stage 디렉터리에서 연 세션은 상위 CLAUDE.md를 상속하므로, 루트 CLAUDE.md에는 발표 중 Agent에게 노출돼도 되는 내용(실행 방법, 스냅샷 규칙, "어려운 용어 안 쓰기"·"난수 금지" 한 줄씩)만 둔다 — 난이도 원칙은 데모 Agent의 말투에 도움이 되고, 난수 금지는 무해해서 남긴다. 단 "모든 숫자는 server/scenarios/에 있다"는 경로 문장은 스포일러 지름길이라 제외. 정본 문서 목록·난이도 원칙·결정론 등 개발 규칙은 이 파일과 CONTEXT.local.md가 소유 — 개발 세션은 이 파일을 먼저 읽는 것으로 시작한다. 스포일러(이상 well, guardrail 숫자) 경로를 데모 세션 컨텍스트에서 제거하기 위함. (2026-08-31)
26. **프롬프트 모음 패널**: 발표자 대본의 주 사용 경로는 대시보드의 "프롬프트 모음" 토글 패널 — 슬래시 커맨드는 프롬프트 원문이 청중에게 안 보이는 단점이 있어, 패널에서 복사해 터미널에 붙여넣는 방식을 기본으로 한다(프롬프트 전문이 터미널에 노출됨). 정본은 여전히 각 stage의 `.claude/commands/*.md` 파일 하나 — 서버 `GET /prompts`가 이 파일들을 읽어 패널에 내려준다(대본 이원화 방지). 슬래시 커맨드도 계속 동작(시작 킷 실습용). 패널은 기본 접힘, 우측 슬라이드 오버(레이아웃 안 밀어냄), stage별 그룹, 항목 = 이름+설명(frontmatter)+본문+복사 버튼. 그룹에는 그 stage에서 처음 등장하는 프롬프트만 노출(누적분 제외 — 재사용 대사는 이전 stage 그룹에서 복사). 커맨드 파일은 전부 `disable-model-invocation: true`(복붙 시 스킬 로드 지연 방지, 수동 `/이름` 호출만). (2026-08-31)
27. **stage7 MHS는 구현 보류**: stage0~6 완성 후 스펙 재개. 방향성 메모 — manifest(자연어 description + procedures params max = 제조사 선언 safety limit), 원심분리기는 조작까지·HPLC는 발견만, 범용 read_device/run_device는 신규 기기 전용(기존 기기는 전용 도구 = custom 연동의 은유).
28. **batch 전환은 상태 보존형**: `next_batch()`는 전환 직전 현재 batch의 상태를 인메모리 스냅샷으로 저장하고, 대상 batch에 저장본이 있으면 복원·없으면 시나리오에서 신선하게 로드한다. 발표 흐름(week1→week2 첫 전환 = 신선한 새 배치)은 그대로, week2→week1 복귀(리허설·개발용)는 진행 상태가 살아 돌아온다. "리셋"은 현재 batch만 시나리오 초기값으로 되돌리고 그 batch의 저장본도 폐기한다. trace도 batch별 스냅샷에 포함해 함께 저장·복원. UI는 헤더의 batch 칩+"다음 배치 투입" 버튼을 세그먼트 토글(week1 | week2)로 통합 — 전환이 파괴가 아니라 이동임을 표현. 스냅샷은 인메모리 전용(파일 저장 없음, 난수 없음 — 결정론 유지). (2026-08-31)
29. **lab_notebook.md는 공통 리소스**: 정본을 `resources/lab_notebook.md` 하나로 두고, stage3+ 디렉터리의 사본은 제거한다(결정 14·21의 stage별 사본 규격을 이 부분만 개정 — 파일 중복 스냅샷 원칙의 예외). stage3+의 CLAUDE.md는 멘션 한 줄이 아니라 "메모리(실험 노트)는 lab_notebook.md를 본다"는 서술 + `@../../resources/lab_notebook.md` import로 구성. 대시보드에도 실험 노트를 노출 — 서버 `GET /labnote`가 `resources/lab_notebook.md`를 읽어 내려주고(요청마다 새로 읽어 발표 중 수정 즉시 반영, /prompts와 같은 방식), 헤더 아이콘 버튼으로 여는 슬라이드오버 패널로 표시. `scripts/check_stages.py`의 CLAUDE.md/lab_notebook.md 검사도 새 규격으로 갱신. (2026-08-31)
